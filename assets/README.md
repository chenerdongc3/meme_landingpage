# Assets

V1 intentionally uses no external runtime assets. When a real product model is ready, place the optimized `.glb` and any compressed textures in this folder and keep the DOM story copy unchanged.
